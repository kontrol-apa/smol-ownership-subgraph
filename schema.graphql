type SmolLandNFT @entity {
  id: ID!
  owner: Bytes! # address
  tokenId: Int!
  image: String!
  offset: Int!
}

type SmolJoeNFT @entity {
  id: ID!
  owner: Bytes! # address
  tokenId: Int!
  image: String!
  offset: Int!
}

type SmolApaNFT @entity {
  id: ID!
  owner: Bytes! # address
  tokenId: Int!
  image: String!
  offset: Int!
}
